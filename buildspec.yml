version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 10
    commands:
      - npm install
      - export CI=true
  build:
    commands:
      - npm run build
      - aws s3 cp ./build s3://$(WEBISTE_BUCKET) --recursive
cache:
  paths:
    - node_modules/**/*